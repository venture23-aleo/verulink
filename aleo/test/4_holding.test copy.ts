describe("Token", () => {
    describe("Hold Fund", () => {
        test.todo("Holds the right amount to right user")
        test.todo("Can only be called from admin")
    })
    describe("Release Fund", () => {
        test.todo("Release the right amount to right user")
        test.todo("Can only be called from admin")
    })
})