---
# Staging Environment Inventory
all:
  hosts:
    staging-attestor-01:
      ansible_host: 34.27.98.138
      ansible_user: ubuntu
      ansible_ssh_private_key_file: ~/Developer/cloud-native-nepal/Training/lab-terraform-vm-ssh
      # Per-attestor configuration
      attestor_name: "stg_attestor_verulink_v23_01"
      attestor_secret_name: "stg/verulink/attestor1/secrets"  # Optional: defaults to env-based name if not specified
      attestor_role_name: "verulink-attestor1-staging-role"  # Optional: defaults to env-based name if not specified
      attestor_profile_name: "verulink-attestor1-staging-profile"  # Optional: defaults to env-based name if not specified
    staging-attestor-02:
      ansible_host: 34.27.107.153
      ansible_user: ubuntu
      ansible_ssh_private_key_file: ~/Developer/cloud-native-nepal/Training/lab-terraform-vm-ssh
      # Per-attestor configuration
      attestor_name: "stg_attestor_verulink_v23_02"
      attestor_secret_name: "stg/verulink/attestor2/secrets"  # Optional: defaults to env-based name if not specified
      attestor_role_name: "verulink-attestor2-staging-role"  # Optional: defaults to env-based name if not specified
      attestor_profile_name: "verulink-attestor2-staging-profile"  # Optional: defaults to env-based name if not specified
  vars:
    env: staging
    infrastructure_provider: gcp  # Options: aws or gcp
    deployment_type: docker  # Options: docker or k8s
    region: us-central1  # AWS region (e.g., us-east-1) or GCP region (e.g., us-central1)
    ansible_user: ubuntu
    gcp_project: playground-s-11-170001e8  # Required if infrastructure_provider is gcp

