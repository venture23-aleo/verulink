name: Create and publish a Docker image

on:
  push:
    tags:
    - '**'
    branches:
    - 'main'
    - '**'

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: attestor 
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Get the Docker image tag
        id: tag
        run: |
          if [ -n "${GITHUB_REF##*refs/tags/*}" ]; then
            echo "tag=$(git rev-parse --short HEAD)" >> $GITHUB_ENV
          else
            echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV
          fi
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: verulink
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: test
        run: pwd & ls
      - name: Build and push Docker image - chainService
        run: |
          docker build -t verulink/chainservice:${{ env.tag }} ./chainService
          docker push verulink/chainservice:${{ env.tag }}

      - name: Build and push Docker image - signingService
        run: |
          docker build -t verulink/signingservice:${{ env.tag }} -f ./signingService/Dockerfile .
          docker push verulink/signingservice:${{ env.tag }}

      - name Print Docker images
        run: | 
          echo "verulink/chainservice:${{ env.tag }}"
          echo "verulink/signingservice:${{ env.tag }}"
      